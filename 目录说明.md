# 目录说明

## 项目层级

```text
repo-root/
  .claude-plugin/
  .codex-plugin/
  README.md
  CHANGELOG.md
  目录说明.md
  ocr-asr-evaluation.md
  skills/
```

Codex 插件根位于：`repo-root/`

skill 位于：`skills/wx-video-account-notes/`

## 目录地图

```text
.claude-plugin/
  plugin.json
  marketplace.json

.codex-plugin/
  plugin.json

skills/
  wx-video-account-notes/
    SKILL.md
    scripts/
    runtime/
    resources/
    .runtime/
    tests/
    examples/
```

## 文件职责

### `skills/wx-video-account-notes/SKILL.md`

skill 主说明，供模型执行时优先读取。

### `.codex-plugin/`

Codex 插件元数据目录。

- `plugin.json`：插件清单

### `.claude-plugin/`

Claude Code 插件元数据目录。

- `plugin.json`：Claude Code 插件清单
- `marketplace.json`：Claude Code marketplace 定义

### `scripts/`

PowerShell 入口脚本。

- `bootstrap.ps1`：准备私有运行时（Python + venv + 依赖 + 模型资产）
- `invoke_pipeline.ps1`：执行主流程（下载→抽帧→OCR→ASR→材料→笔记）
- `verify_runtime.ps1`：检查运行时是否就绪

### `runtime/`

Python 源码。

- `resolve_link.py`：解析视频号分享链接
- `download_video.py`：下载视频
- `media_extract.py`：抽帧和抽音频
- `ocr_runner.py`：OCR（RapidOCR + 跳帧 + 提前退出优化）
- `asr_runner.py`：ASR（faster-whisper CPU）
- `compose_note.py`：生成 `note_materials.json`；无语音视频自动附加 `visual_frames`
- `pipeline.py`：主流程编排
- `bootstrap.py`：资产下载和安装

### `resources/`

模型成稿参考文件。

- `model-note-template.md`

### `.runtime/`

自动生成的私有运行环境，包含工具（ffmpeg）、模型（faster-whisper tiny）、Python 虚拟环境和缓存。

### `tests/`

测试说明和 Python 单元测试。

### `examples/`

使用示例。

### `ocr-asr-evaluation.md`

OCR / ASR 实测记录与历史对比。
